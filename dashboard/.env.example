# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# API — Backend base URL (no trailing slash)
# Development: leave unset (falls back to http://localhost:8000)
# Production (Vercel proxy): set to empty string
# Production (direct):       set to https://your-backend.up.railway.app
VITE_API_URL=
